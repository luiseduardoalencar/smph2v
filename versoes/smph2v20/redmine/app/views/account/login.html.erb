<%= call_hook :view_account_login_top %>

<div id="login-form" style="display: flex; flex-direction: column; align-items: center;">

  <%= image_tag('logo_horizontal_azul.png', class: 'login-logo', style: 'max-width: 100%; height: auto;') %>

  <%= form_tag(signin_path, onsubmit: 'return keepAnchorOnSignIn(this);', id: 'login-form', style: 'width: 400px; margin: 50px 0;') do %>
    <%= back_url_hidden_field_tag %>

    <label for="username"><%= l(:field_login) %></label>
    <%= text_field_tag 'username', params[:username], tabindex: '1' %>

    <label for="password">
      <%= l(:field_password) %>
    </label>
    <%= password_field_tag 'password', nil, tabindex: '2' %>

    <% if Setting.autologin? %>
      <label for="autologin"><%= check_box_tag 'autologin', 1, false, tabindex: 4 %> <%= l(:label_stay_logged_in) %></label>
    <% end %>

    <input type="submit" name="login" value="<%= l(:button_login) %>" tabindex="5" id="login-submit" />
  <% end %>
</div>

<%= call_hook :view_account_login_bottom %>

<% if params[:username].present? %>
  <%= javascript_tag "$('#password').focus();" %>
<% else %>
  <%= javascript_tag "$('#username').focus();" %>
<% end %>